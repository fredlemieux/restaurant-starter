link-workspace-packages=true
prefer-workspace-packages=true
auto-install-peers=true

# This is an open-source scaffold — pin versions explicitly and resolve fresh.
# The global ~/.npmrc release-age policy doesn't apply during local scaffold dev.
minimum-release-age=0

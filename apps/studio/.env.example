# Sanity Studio only reads env vars prefixed `SANITY_STUDIO_`.
# The web app uses different (unprefixed) names — see apps/web/.env.example.
SANITY_STUDIO_PROJECT_ID=gy9z4n0h
SANITY_STUDIO_DATASET=production
